import 'package:flutter/material.dart';

class MultipleThemePage extends StatelessWidget {
  const MultipleThemePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Multiple Theme"),
      ),
    );
  }
}
